astroCopy code
---
// src/components/PostCard.astro
const { post } = Astro.props;
---

<div class="post-card">
  <h2>{post.title}</h2>
  <p>{post.body}</p>
  <a href={`/posts/${post.id}`}>Ver detalles</a>
</div>

